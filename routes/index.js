const homeRoutes = require('./home')
const addRoutes = require('./add')
const coursesRoutes = require('./courses')
const cartRoutes = require('./cart')

module.exports = {
    homeRoutes, addRoutes, coursesRoutes, cartRoutes
}